<%= render :partial => 'layouts/header' %>

<main>
  <section class="edit-animal">
    <div class="container">
		<h1>Editing Image</h1>

		<%= render 'form', animal: @animal, image: @image %>

		<%= link_to 'Show', @image %> |
		<%= link_to 'Back', animal_images_path %>
    </div>  
  </section>
</main>
